#include <stdio.h>

int main() {
    int A, B;

    scanf("%d %d", &A, &B); // 사용자에게 두 정수 A, B를 입력받음

    printf("%d\n", A * B); // A와 B의 곱하기 결과 출력

    return 0; // main() 함수의 모든 명령문을 수행한 후 0을 외부로 변환함
}