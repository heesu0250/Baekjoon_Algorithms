#include <iostream>
using namespace std; // std 네임스페이스를 생략하여 사용

int main() {
    int A, B; // 두 정수를 저장할 변수 A와 B 선언

    // 사용자로부터 두 정수를 입력받음
    cin >> A >> B;

    // 두 정수의 곱을 출력
    cout << A * B << endl;

    return 0;
}